/**
 * @educorvi/uv-theme/components — Vue component exports
 *
 * This entry is processed by Vite/bundler only (never by bare Node ESM).
 * Import in .vue files or in Vite-processed modules:
 *
 *   import { ThemeSwitcher } from '@educorvi/uv-theme/components';
 */
export { default as ThemeSwitcher } from './ThemeSwitcher.vue';
