import { defaultComponents } from '@/renderings/default/DefaultComponents.ts';
import { bootstrapComponents } from '@/renderings/bootstrap/BootstrapComponents.ts';

export { defaultComponents, bootstrapComponents };
